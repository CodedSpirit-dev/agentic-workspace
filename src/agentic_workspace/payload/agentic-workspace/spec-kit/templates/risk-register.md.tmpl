# {PROJECT_TITLE} Risk Register

Typed risks live under `registry/risks/`. Each risk must state probability,
impact, trigger, owner, mitigation, contingency, and current disposition.

Create a risk with:

```bash
agentic-workspace/spec-kit/bin/project-kit create <project> risk --title "<risk>"
```
