# Findings — {PROJECT_TITLE}

Typed findings live under `findings/` and retain stable `FND-*` IDs.

Each finding records the observation date, source, exact evidence, confidence,
impact, disposition, and related artifacts. Mark unsupported claims
`(unverified)` and state what evidence would resolve them. Preserve rejected
hypotheses with the evidence that rejected them.
