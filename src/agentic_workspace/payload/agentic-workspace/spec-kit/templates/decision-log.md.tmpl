# Decision Log — {PROJECT_TITLE}

Typed decisions live under `registry/decisions/` and retain stable `DEC-*` IDs.
Create and supersede them with `project-kit decision`; never rewrite accepted
history.

Every decision records date, owner, forcing context, evidence, alternatives,
choice, consequences, reversal conditions, and affected artifact relations.
