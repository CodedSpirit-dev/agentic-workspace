# Projects

Create governed initiatives here with `spec-kit/bin/project-kit`. Do not create
project folders manually; the CLI owns the registry and mode-specific shape.

Use `traditional` for ordered phases, `sprint` for timeboxed increments, and
`flexible` for bounded workstreams. See
[`../docs/project-management/index.md`](../docs/project-management/index.md).
