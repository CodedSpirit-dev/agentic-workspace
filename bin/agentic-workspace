#!/usr/bin/env bash
set -euo pipefail
PRODUCT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
export PYTHONPATH="$PRODUCT_DIR/src${PYTHONPATH:+:$PYTHONPATH}"
exec python3 -m agentic_workspace.cli "$@"
